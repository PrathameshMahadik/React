// Create a React component (Modal) that displays a modal dialog with content.
// Users can open and close the modal. Write tests to check if the modal opens and closes when triggered.
import React from "react";
import Model from "../../Day5/Components/Model";

const Task6 = () => {
  return (
    <>
      <Model a="Welcome to successive" b="I am a trainee Engineer" />
    </>
  );
};
export default Task6;
